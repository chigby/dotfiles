# -*- mode: conf -*-
[user]
    name = Cameron Higby-Naquin
    email = cameron.higbynaquin@gmail.com
[alias]
    co = checkout
    st = status
    dc = diff --cached
    ci = commit
    di = diff
    pl = pull --ff-only
    fa = fetch --all
    ff = merge --ff-only
    noff = merge --no-ff

    r = !git l -25
    l = "!. ~/.githelpers && pretty_git_log"
    la = !git l --all

[core]
    editor = emacsclient
[color]
    ui = true
[push]
    default = simple
